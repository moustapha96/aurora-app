# Configuration Supabase
# Copiez ce fichier en .env et remplissez les valeurs

# URL de votre projet Supabase
# Trouvable dans Settings > API > Project URL
VITE_SUPABASE_URL=https://votre-projet.supabase.co

# Clé publique (anon key) de Supabase
# Trouvable dans Settings > API > anon public key
VITE_SUPABASE_PUBLISHABLE_KEY=votre-anon-key-ici

# Optionnel : Clé pour l'analyse de carte d'identité (si utilisée)
# LOVABLE_API_KEY=votre-lovable-api-key

# ============================================
# Configuration Edge Functions (Supabase Dashboard)
# ============================================
# Ces variables doivent être configurées dans Supabase Dashboard > Edge Functions > Settings

# Environnement (production ou development)
# ENVIRONMENT=production

# Domaines autorisés pour CORS (séparés par des virgules)
# Domaines autorisés: localhost:8080 et aurorasociety.ch
# Exemple:
# ALLOWED_ORIGINS=http://localhost:8080,https://aurorasociety.ch,https://www.aurorasociety.ch

